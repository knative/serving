#@ load("@ytt:data", "data")
#@ load("@ytt:overlay", "overlay")

#@ def namespace(name):
kind: Namespace
metadata:
  name: #@ name
#@ end

#@ serving_ns =     overlay.subset(namespace(data.values.serving.namespaces.test.default))
#@ alt_serving_ns = overlay.subset(namespace(data.values.serving.namespaces.test.alternate))
#@ tls_ns =         overlay.subset(namespace(data.values.serving.namespaces.test.tls))

#@overlay/match by=overlay.or_op(serving_ns, alt_serving_ns, tls_ns),expects="3+"

#@overlay/remove
