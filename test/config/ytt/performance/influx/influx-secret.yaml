#@ load("@ytt:base64", "base64")
#@ load("@ytt:data", "data")

---
apiVersion: v1
kind: Secret
metadata:
  name: influx-secret
data:
  token: #@ base64.encode("{}".format(data.values.influxtoken))
  url: #@ base64.encode("{}".format(data.values.influxurl))
